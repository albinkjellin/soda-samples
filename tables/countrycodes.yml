table_name: countrycodes
metrics:
  - invalid_percentage
tests:
  - row_count > 0
columns:
  countrycode:
    valid_values:
      - US
      - UK
      - Netherlands
    tests:
      - invalid_percentage == 0
